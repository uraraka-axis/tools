# 新規画像取得 全体設計

## 現状の課題

### 機能が分散している
```
現状のモード構成:
├── 📁 画像一覧取得      → R-Cabinetの画像を確認
├── 🔍 画像存在チェック  → 不足画像を特定 → GitHubアップロード
└── 🖼️ 新規画像取得      → 画像をダウンロード
```

**問題点:**
- ユーザーが複数モードを行き来する必要がある
- 存在チェック → 画像取得 の流れが分断されている
- 単品対応が不完全
- アップロード（楽天/ヤフー）は別ツールが必要

### 役割分担による非効率
```
現状: Aさん(取得) → Bさん(バッジ) → Cさん(アップ)
理想: 1人で完結
```

---

## 設計方針

### 案1: 統合型（推奨）

すべてを1つのワークフローに統合:

```
🖼️ 画像ワークフロー（統合モード）
│
├── Step 1: 不足画像の特定
│   └── R-Cabinet存在チェック（自動）
│
├── Step 2: 画像取得＋加工
│   ├── セット品: 画像取得 + 送料無料バッジ
│   └── 単品: 画像取得のみ
│
├── Step 3: アップロード準備
│   ├── 楽天用: フォルダ振り分け
│   └── ヤフー用: リネーム + ZIP
│
└── Step 4: API連携
    ├── 楽天: cabinet.file.insert
    └── ヤフー: uploadItemImagePack
```

**メリット:**
- 1人で最初から最後まで完結
- 途中で人間確認を挟める
- 進捗が明確

### 案2: 分離型（現状ベース改善）

既存モードを維持しつつ改善:
- 画像存在チェック: 単品対応追加
- 新規画像取得: バッジ＋アップロード追加

**デメリット:**
- 行き来が残る
- 一貫性がない

---

## 推奨案: 統合型の詳細設計

### 運用モード

```
【自動実行】毎朝8時（最初は週3）
Step ①〜④ を自動実行
    ↓
【手動】9時出社後
Step ⑤: API連携（人間確認後にボタンクリック）
    ↓
【自動】アップロード完了後
蓄積データ（missing_comics.csv、is_list.csv、comic_list.csv）をクリア

【イレギュラー対応】
Step ①〜④ は個別に手動実行も可能
```

### UI構成

```
サイドバー:
├── 📁 画像一覧取得（既存・変更なし）
└── 🖼️ 画像ワークフロー（統合・新規）
    ※ 旧「画像存在チェック」と「新規画像取得」を統合
```

### 5ステップ構成

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 ① 不足特定   ② JAN取得   ③ 画像取得   ④ 準備   ⑤ アップロード
    ●━━━━━━━━━○━━━━━━━━━○━━━━━━━━━━○━━━━━━━━○
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

| Step | 名称 | 処理内容 | 実行 |
|------|------|----------|------|
| ① | 不足特定 | R-Cabinet存在チェック → missing_comics.csv | 自動 |
| ② | JAN取得 | コミックリスター → is_list.csv / comic_list.csv | 自動 |
| ③ | 画像取得 | 画像ダウンロード＋バッジ合成 | 自動 |
| ④ | 準備 | 楽天フォルダ振り分け / ヤフーZIP生成 | 自動 |
| ⑤ | アップロード | API連携 | 手動 |
```

---

### Step ①: 不足特定

**目的:** どの画像が不足しているか特定する

**入力方法（2パターン）:**
```
A) コミックNo直接入力
   └── テキストエリアに入力

B) CSVアップロード
   └── コミックNoリストをアップロード
```

**処理:**
```
入力されたコミックNo
    ↓
R-Cabinet DB照合
    ↓
存在チェック結果表示
┌──────────┬────────┬──────────┐
│コミックNo │ 存在   │ 種別     │
├──────────┼────────┼──────────┤
│ 123456   │ ❌なし │ セット品 │
│ 78901    │ ✅あり │ セット品 │
│ 19763_003│ ❌なし │ 単品     │
└──────────┴────────┴──────────┘
    ↓
不足分を missing_comics.csv / missing_tanpin.csv にアップロード
    ↓
Excelダウンロード（手動確認用）
```

**必要なファイル:**
- R-Cabinet画像DB（Supabase）← 既存

**出力:**
- missing_comics.csv（セット品 + 単品のベース）→ GitHub
- missing_tanpin.csv（単品の巻数情報）→ GitHub
- チェック結果Excel（手動ダウンロード用）

---

### Step ②: JAN取得

**目的:** コミックリスターでJAN情報を取得

**処理:**
```
missing_comics.csv（Step ①でアップロード済み）
    ↓
コミックリスター処理（GitHub Actions）
    ↓
is_list.csv / comic_list.csv 生成・更新
```

**必要なファイル:**
- missing_comics.csv ← Step ①で生成

**出力:**
- is_list.csv（全巻のJAN情報）
- comic_list.csv（セット品のJAN情報）

---

### Step ③: 画像取得＋加工

**目的:** 不足画像をダウンロードし、必要な加工を行う

**入力ファイル:**
- is_list.csv（GitHub自動取得）← Step ②で生成済み

**バッジ画像:**
- 場所: `images/badge_free_shipping.jpg`（リポジトリ内）
- 位置: 右上
- 対象: セット品のみ（単品はバッジなし）

**処理フロー:**
```
is_list.csv（Step ②で生成）
    ↓
missing_comics.csv から処理対象を特定
    ↓
┌─────────────────────────────────────┐
│ セット品（コミックNoに _ なし）      │
│ ├── is_list.csv から JAN取得        │
│ │   └── コミックNoで検索 → 最終巻JAN│
│ ├── 画像取得（ブックオフ/Amazon等） │
│ └── 送料無料バッジ合成（右上）      │
└─────────────────────────────────────┘
    ↓
┌─────────────────────────────────────┐
│ 単品（コミックNoに _ あり）          │
│ ├── コミックNo解析: 19763_003       │
│ │   └── ベース: 19763, 巻数: 3      │
│ ├── is_list.csv から JAN取得        │
│ │   └── ベース + 巻数で検索         │
│ └── 画像取得（バッジなし）          │
└─────────────────────────────────────┘
    ↓
画像保存（ローカル一時フォルダ）
```

**画像取得元（優先順）:**
1. ブックオフオンライン（JANで検索）
2. Amazon（JANで検索）
3. 取得失敗時はスキップ＋ログ出力

**バッジ合成処理（Pillow）:**
```python
from PIL import Image

def add_badge(image_path, badge_path, output_path):
    """右上にバッジを合成"""
    base = Image.open(image_path).convert("RGBA")
    badge = Image.open(badge_path).convert("RGBA")

    # バッジサイズ調整（元画像の1/4程度）
    badge_width = base.width // 4
    badge_ratio = badge_width / badge.width
    badge_height = int(badge.height * badge_ratio)
    badge = badge.resize((badge_width, badge_height))

    # 右上に配置
    x = base.width - badge_width - 10  # 右端から10px
    y = 10  # 上端から10px

    base.paste(badge, (x, y), badge)
    base.convert("RGB").save(output_path, "JPEG", quality=95)
```

**出力:**
- セット品: `{コミックNo}.jpg`（バッジ付き）
- 単品: `{コミックNo}_{巻数}.jpg`（バッジなし）
- 保存先: 一時フォルダ（Step ④で使用）

---

### Step ④: アップロード準備

**目的:** 楽天/ヤフー向けにファイルを整形

**入力（手動アップロード必要）:**
```
ヤフー用:
└── データフォーマットExcel（商品コード ↔ コミックNo）
    ├── セット品シート: A列=商品コード, D列=コミックNo
    └── 単品シート: A列=商品コード, E列=コミックNo

楽天用:
└── フォルダ階層リスト.xlsx（コミックNo → フォルダ名）
    └── 形式: コミックNo | フォルダパス
```

---

#### ヤフー準備

**処理フロー:**
```
Step ③の画像（{コミックNo}.jpg）
    ↓
Excelから商品コードをルックアップ
    ↓
画像リネーム: {コミックNo}.jpg → {商品コード}.jpg
    ↓
┌─────────────────────────────────────────────┐
│ セット品のみ追加画像を付与                  │
│ ├── {商品コード}_1.jpg（透明カバー画像）   │
│ └── {商品コード}_2.jpg（aaaaa.jpg）        │
│                                             │
│ ※ 追加画像は事前にリポジトリに配置:        │
│   - images/additional_1.jpg（透明カバー）   │
│   - images/additional_2.jpg（aaaaa）        │
└─────────────────────────────────────────────┘
    ↓
ZIP生成（25MB以下で自動分割）
    ↓
出力: yahoo_upload_001.zip, yahoo_upload_002.zip, ...
```

**ZIP分割ロジック:**
```python
MAX_ZIP_SIZE = 25 * 1024 * 1024  # 25MB

def create_split_zips(image_files, output_dir):
    """25MB以下でZIPを分割生成"""
    zip_index = 1
    current_size = 0
    current_files = []

    for img_path in image_files:
        file_size = os.path.getsize(img_path)
        if current_size + file_size > MAX_ZIP_SIZE and current_files:
            # 現在のZIPを保存
            save_zip(current_files, f"yahoo_upload_{zip_index:03d}.zip")
            zip_index += 1
            current_files = []
            current_size = 0

        current_files.append(img_path)
        current_size += file_size

    # 残りを保存
    if current_files:
        save_zip(current_files, f"yahoo_upload_{zip_index:03d}.zip")
```

---

#### 楽天準備

**処理フロー:**
```
Step ③の画像（{コミックNo}.jpg）
    ↓
楽天 API: cabinet.folders.get でフォルダ一覧取得
    ↓
フォルダ階層リスト.xlsx と突合
    ↓
┌─────────────────────────────────────────────┐
│ マッピング生成                              │
│ コミックNo → フォルダパス → FolderId        │
│                                             │
│ 例:                                         │
│ 12345 → /comic/action/ → folder_abc123     │
│ 67890 → /comic/romance/ → folder_def456    │
└─────────────────────────────────────────────┘
    ↓
アップロードキュー生成: [{file, folderId}, ...]
```

**出力:**
- ヤフー: ZIPファイル群（ダウンロード可能）
- 楽天: アップロードキュー（JSON形式、Step ⑤で使用）

---

### Step ⑤: API連携（手動実行）

**目的:** 楽天/ヤフーに画像をアップロード

**重要:** このステップのみ手動実行（人間確認後にボタンクリック）

---

#### ヤフー uploadItemImagePack API

```
エンドポイント: https://circus.shopping.yahooapis.jp/ShoppingWebService/V1/uploadItemImagePack
メソッド: POST (multipart/form-data)
認証: Authorization: Bearer {access_token}
入力: ZIPファイル（25MB以下、1リクエスト1ZIP）
レート制限: 10,000枚/時間
```

**リクエスト例:**
```python
import requests

def upload_to_yahoo(zip_path, access_token):
    url = "https://circus.shopping.yahooapis.jp/ShoppingWebService/V1/uploadItemImagePack"
    headers = {"Authorization": f"Bearer {access_token}"}

    with open(zip_path, "rb") as f:
        files = {"file": (os.path.basename(zip_path), f, "application/zip")}
        response = requests.post(url, headers=headers, files=files)

    return response.status_code == 200
```

**認証設定（Streamlit Secrets）:**
```toml
[yahoo]
access_token = "取得したトークン"
```

---

#### 楽天 cabinet.file.insert API

```
エンドポイント: https://api.rms.rakuten.co.jp/es/2.0/cabinet/file/insert
メソッド: POST (multipart/form-data)
認証: ESA {base64(serviceSecret:licenseKey)}
入力: file（バイナリ）, folderId, fileName
レート制限: 1ファイル/秒程度（推奨）
```

**リクエスト例:**
```python
import requests
import base64

def upload_to_rakuten(file_path, folder_id, service_secret, license_key):
    url = "https://api.rms.rakuten.co.jp/es/2.0/cabinet/file/insert"

    auth_str = f"{service_secret}:{license_key}"
    auth_b64 = base64.b64encode(auth_str.encode()).decode()
    headers = {"Authorization": f"ESA {auth_b64}"}

    with open(file_path, "rb") as f:
        files = {"file": (os.path.basename(file_path), f, "image/jpeg")}
        data = {"folderId": folder_id, "fileName": os.path.basename(file_path)}
        response = requests.post(url, headers=headers, files=files, data=data)

    return response.status_code == 200
```

---

#### UI設計

```
┌─────────────────────────────────────────────────────────────┐
│ Step ⑤: アップロード実行                                   │
│                                                             │
│ ┌─────────────────────┐  ┌─────────────────────┐           │
│ │ 🛒 ヤフー           │  │ 🏪 楽天             │           │
│ │                     │  │                     │           │
│ │ 対象: 25件 (3 ZIP)  │  │ 対象: 25件          │           │
│ │ [アップロード開始]  │  │ [アップロード開始]  │           │
│ └─────────────────────┘  └─────────────────────┘           │
│                                                             │
│ 進捗: ████████████░░░░░░░░ 60% (15/25件)                   │
│                                                             │
│ ログ:                                                       │
│ 09:15:32 ✅ yahoo_upload_001.zip → 成功 (8件)              │
│ 09:15:45 ✅ yahoo_upload_002.zip → 成功 (8件)              │
│ 09:15:58 ❌ yahoo_upload_003.zip → エラー: 認証失敗        │
│                                                             │
│ [失敗分を再試行]  [ログをダウンロード]                      │
└─────────────────────────────────────────────────────────────┘
```

---

#### アップロード完了後の処理

```
アップロード成功
    ↓
蓄積CSVをクリア:
├── comic-lister/data/missing_comics.csv → 空に
├── comic-lister/data/missing_tanpin.csv → 空に
└── 一時画像フォルダ → 削除
    ↓
完了通知（Streamlit上に表示）
```

**クリア処理:**
```python
def clear_accumulated_data():
    """アップロード完了後、蓄積データをクリア"""
    files_to_clear = [
        "comic-lister/data/missing_comics.csv",
        "comic-lister/data/missing_tanpin.csv",
    ]

    for file_path in files_to_clear:
        # ヘッダー行のみ残して空にする
        upload_empty_csv_to_github(file_path)

    # 一時画像フォルダを削除
    shutil.rmtree(TEMP_IMAGE_DIR, ignore_errors=True)
```

---

## 必要なファイル一覧（整理）

### GitHub自動取得
| ファイル | 用途 | 生成元 |
|---------|------|--------|
| is_list.csv | JAN検索（セット品・単品両方） | Step ② Comic Lister |
| comic_list.csv | セット品情報 | Step ② Comic Lister |
| missing_comics.csv | 処理対象リスト | Step ① 不足特定 |
| missing_tanpin.csv | 単品の巻数情報 | Step ① 不足特定 |

### リポジトリ配置済み
| ファイル | 用途 | 配置場所 |
|---------|------|----------|
| badge_free_shipping.jpg | 送料無料バッジ | images/ |
| additional_1.jpg | ヤフー追加画像1（透明カバー）| images/ ※要配置 |
| additional_2.jpg | ヤフー追加画像2（aaaaa）| images/ ※要配置 |

### 手動アップロード（Step ④で必要）
| ファイル | 用途 | 形式 |
|---------|------|------|
| フォルダ階層リスト.xlsx | 楽天フォルダ振り分け | コミックNo → フォルダパス |
| データフォーマットExcel | ヤフー商品コードマッピング | 商品コード ↔ コミックNo |

---

## 認証情報（必要なもの）

| サービス | 認証方式 | 現状 |
|---------|---------|------|
| 楽天 RMS | ESA (serviceSecret + licenseKey) | ✅ 既存ツールで設定済み |
| ヤフー | Bearer トークン | ❌ 新規設定が必要 |
| GitHub | トークン | ✅ 既存ツールで設定済み |

---

## UI/UX設計

### ステップ表示
```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  ① 不足特定    ② 画像取得    ③ 準備    ④ アップロード
     ●━━━━━━━━━━━○━━━━━━━━━━━○━━━━━━━━━━━○
   [現在地]
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

### 各ステップで「完了して次へ」または「ここで終了」を選択可能
- 画像取得だけして終わりたい → ZIPダウンロードで終了
- 楽天だけアップしたい → ヤフー準備をスキップ

---

## 削除・統合するモード

| モード | 対応 |
|--------|------|
| 📁 画像一覧取得 | **残す**（確認用途として独立） |
| 🔍 画像存在チェック | **統合**（新ワークフローのStep 1へ） |
| 🖼️ 新規画像取得 | **統合**（新ワークフローへ） |

---

## 実装フェーズ

### Phase 1: 基盤整備
- [ ] 新UI構造の作成（ステップ表示）
- [ ] 既存機能の移植（存在チェック、画像取得）

### Phase 2: 単品対応
- [ ] 単品の存在チェック
- [ ] 単品のJAN検索（is_list.csv）
- [ ] 単品の画像取得

### Phase 3: バッジ合成
- [ ] 送料無料バッジ画像の管理
- [ ] Pillow による画像合成

### Phase 4: アップロード準備
- [ ] ヤフー用リネーム＋ZIP生成
- [ ] 楽天用フォルダIDマッピング

### Phase 5: API連携
- [ ] ヤフー uploadItemImagePack 実装
- [ ] 楽天 cabinet.file.insert 実装
- [ ] ヤフー認証設定UI

---

## 決定事項

1. **送料無料バッジ画像** ✅ 解決済み
   - 場所: `images/badge_free_shipping.jpg`（リポジトリ内）
   - 位置: 右上
   - 対象: セット品のみ

2. **Step ③の入力ファイル** ✅ 解決済み
   - is_list.csv のみ（GitHub自動取得）

---

## 未決定事項

1. **ヤフー認証トークンの取得方法**
   - OAuth 2.0フロー？
   - 手動でトークンを取得してSecrets登録？

2. **追加画像（_1, _2）の元ファイル**
   - 透明カバー画像: どこから取得？
   - aaaaa.jpg: どこから取得？
   - → リポジトリに `images/additional_1.jpg`, `images/additional_2.jpg` として配置予定

---

## 次のアクション

この設計でよければ:
1. 各項目の詳細を確認・調整
2. 実装フェーズ1から着手

フィードバックがあれば修正します。
